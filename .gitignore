# ----------------------------------
# Secrets — ห้ามขึ้น git เด็ดขาด
# ----------------------------------
.env

# ----------------------------------
# ข้อมูลสินค้าที่ scrape มา (~255 MB) — ใหญ่เกินไปสำหรับ git
# รัน scraping/get_categories.py + scraping/scrap_all_product.py
# เพื่อสร้างข้อมูลชุดนี้ใหม่แทน (ดูวิธีใน README.md)
# ----------------------------------
data/all_product/

# ----------------------------------
# Python
# ----------------------------------
__pycache__/
*.pyc
*.pyo
*.egg-info/
*.egg

# ----------------------------------
# Virtual environment (ชื่อโฟลเดอร์ venv มักตั้งชื่อไม่เหมือนกัน
# ใส่ pattern ที่พบบ่อยไว้ครอบคลุม รวมถึงแบบที่ตั้งชื่อด้วยรหัสนักศึกษา
# เช่น env_6610110018 ที่เห็นใช้อยู่)
# ----------------------------------
venv/
.venv/
env/
ENV/
env_*/

# ----------------------------------
# OS / Editor
# ----------------------------------
.DS_Store
Thumbs.db
.vscode/
.idea/